# Workspace Rules

- ALWAYS use the `ponytail` skill for all coding and problem-solving tasks. Default to the simplest, shortest, most minimal solution that works.
- ALWAYS apply the `caveman-compression` skill to all responses, compressing outputs and avoiding unnecessary grammar to save tokens.
